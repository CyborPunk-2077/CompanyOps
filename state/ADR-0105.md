# ADR-0105 — Browser Progress Uses Same-Origin SSE Proxy
Status: Accepted

Development API principal/auth context remains server-side. The browser opens EventSource only to the same-origin Next.js progress proxy. The only new client island is progress transport/presentation; it cannot calculate Business CI semantic truth.
