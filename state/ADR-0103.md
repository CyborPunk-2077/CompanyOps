# ADR-0103 — Live Server Result Source With Explicit Fixture Fallback
Status: Accepted

The Next.js server layer prefers persisted API cockpit/chronology/consequence/replay views when live API configuration and a sealed experiment are available. Projection Golden remains a deterministic offline/demo/browser-QA fallback and must be visibly labeled as such.
